<div class="v2-pinned-card" data-pin-key="{{ $key }}" data-empty-text="{{ translate('Hover any item and tap the pin to add a shortcut here.') }}">
    <div class="v2-pinned-header">
        <span class="v2-pinned-icon">@include('layouts.admin.partials._v2_pin_icon')</span>
        <span>{{ translate('Pinned') }}</span>
    </div>
    <div class="v2-pinned-list"></div>
</div>
